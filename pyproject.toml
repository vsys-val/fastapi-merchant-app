[project]\nname = "fastapi-merchant-app"\nversion = "0.1.0"\ndescription = "API de catálogo compartilhado e avaliações de produtos"\nrequires-python = ">=3.12"\n\n[tool.pytest.ini_options]\ntestpaths = ["tests"]\npythonpath = ["."]\n